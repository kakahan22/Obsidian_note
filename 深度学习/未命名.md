# 层和块
	利用nn.sequential定义了一种特殊的Module，任何的神经网络都是Module的子类。
	当sequential无法满足我们的需要时，可以自己构造我们的Net网络和层。
# 参数逻辑
访问模型里面的数据可以多样化
```
import torch
from torch import nn

net = nn.Sequential(nn.Linear(4,8),nn.ReLU(),nn.Linear(8,1))
X = torch.rand(size=(2,4))
net(X)
//tensor([[0.0511],
        [0.0924]], grad_fn=<AddmmBackward0>)
#net[a]表示前面所定义的网络的层数，比如2是第二层。
# state_dict() 是指weight和bias
print(net[2].state_dict())
//OrderedDict([('weight', tensor([[ 0.1472, -0.1559,  0.0771, -0.0689, -0.1874,  0.1543, -0.1615, -0.0244]])), ('bias', tensor([0.0463]))])
```
访问目标参数Parameter，比如bias，weight.
也可以访问所有的参数Parameters
```
print(type(net[2].bias))
print(net[2].bias)
print(net[2].bias.data)
print(net[2].weight.grad)
print(net[2].weight)
//
<class 'torch.nn.parameter.Parameter'>
Parameter containing:
tensor([0.0463], requires_grad=True)
tensor([0.0463])
None
Parameter containing:
tensor([[ 0.1472, -0.1559,  0.0771, -0.0689, -0.1874,  0.1543, -0.1615, -0.0244]],
       requires_grad=True)

print([(name,param.shape) for name, param in net[2].named_parameters()])
print([(name,param.shape) for name,param in net.named_parameters()])
net.state_dict()['2.bias'].data
//
[('weight', torch.Size([1, 8])), ('bias', torch.Size([1]))]
[('0.weight', torch.Size([8, 4])), ('0.bias', torch.Size([8])), ('2.weight', torch.Size([1, 8])), ('2.bias', torch.Size([1]))]
tensor([0.0463])
```
模型可以通过嵌套块建立，可以通过嵌套块收集参数
```
def block1():
    return nn.Sequential(nn.Linear(4,8),nn.ReLU(),nn.Linear(8,4))

def block2():
    net=nn.Sequential()
    for i in range(4):
        # add_module可以添加一句话进行描述
        net.add_module(f'block{i}',block1())
    return net
rgnet = nn.Sequential(block2(),nn.Linear(4,1))
rgnet(X)
print(rgnet)
//
Sequential(
  (0): Sequential(
    (block0): Sequential(
      (0): Linear(in_features=4, out_features=8, bias=True)
      (1): ReLU()
      (2): Linear(in_features=8, out_features=4, bias=True)
    )
    (block1): Sequential(
      (0): Linear(in_features=4, out_features=8, bias=True)
      (1): ReLU()
      (2): Linear(in_features=8, out_features=4, bias=True)
    )
    (block2): Sequential(
      (0): Linear(in_features=4, out_features=8, bias=True)
      (1): ReLU()
      (2): Linear(in_features=8, out_features=4, bias=True)
    )
    (block3): Sequential(
      (0): Linear(in_features=4, out_features=8, bias=True)
      (1): ReLU()
      (2): Linear(in_features=8, out_features=4, bias=True)
    )
  )
  (1): Linear(in_features=4, out_features=1, bias=True)
)
```
内置初始化
```
def init_normal(m):
    if type(m) == nn.Linear:
        nn.init.normal_(m.weight,mean = 0 ,std = 0.01)
        nn.init.zeros_(m.bias)

net.apply(init_normal)
net[0].weight.data[0],net[0].bias.data[0]
//(tensor([-0.0029, -0.0179, -0.0001,  0.0017]), tensor(0.))
```

```
def init_constant(m):
    """将我们的weight设置为常数，尽量不要这么用！！！！"""
    if type(m)==nn.Linear:
        nn.init.constant(m.weight,1)
        nn.init.zeros_(m.bias)

net.apply(init_constant)
net[0].weight.data[0],net[0].bias.data[0]
//(tensor([1., 1., 1., 1.]), tensor(0.))
```
对应不同初始化方法，比如之前提到的Xavier，init_42,kaiming/he(主要用于relu函数)
```
def Xavier(m):
    if type(m)==nn.Linear:
        nn.init.xavier_uniform_(m.weight)

def init_42(m):
    if type(m) == nn.Linear:
        nn.init.constant_(m.weight,42)

def init_weights(m):
    if isinstance(m, nn.Linear):
        nn.init.kaiming_normal_(m.weight, nonlinearity='relu')
        nn.init.zeros_(m.bias)

net[0].apply(Xavier)
net[2].apply(init_42)
print(net[0].weight.data[0])
print(net[2].weight.data)
//tensor([-0.4614, -0.3863, -0.4521, -0.2757])
tensor([[42., 42., 42., 42., 42., 42., 42., 42.]])
```

```
def my_init(m):
    """自己定义我们的初始化"""
    if type(m)==nn.Linear:
        print(
            "Init",
            *[(name,param.shape) for name,param in m.named_parameters()][0])
        nn.init.uniform_(m.weight,-10,10)
        m.weight.data *=m.weight.data.abs()>=5

net.apply(my_init)
net[0].weight[:2]
        
//Init weight torch.Size([8, 4])
Init weight torch.Size([1, 8])
tensor([[ 0.0000, -0.0000, -7.8616,  0.0000],
        [-9.2199, -7.1911, -0.0000, -0.0000]], grad_fn=<SliceBackward0>)
```
参数绑定，我们在模型中会有需要共享的一个层，就是两个数据流经过同一个模型层，这个层的参数是一样的。
```

```

