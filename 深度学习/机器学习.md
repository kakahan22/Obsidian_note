# 线性回归
线性表达，y=wx+b的部分，不多赘述
## 线性回归纯手搓
```
%matplotlib inline

import random

import torch

# d2l是一个深度学习的入门教学库

from d2l import torch as d2l
```
首先根据带有噪声的线性模型构造一个人造数据集。我们使用线性模型参数w=[2,-3.4]T , b=4.2和噪声项$\epsilon$生成数据集及其标签：

    y=Xw+b+$ \epsilon$
   ```
   def synthetic_data(w, b, num_examples):

    """生成y = Xw + b + 噪声"""

    X=torch.normal(0, 1, (num_examples, len(w)))

    #随机生成符合正态分布的数据,torch.normal(mean均值, std标准差, size=None输出的张量形状, *, generator=None, out=None)

    Y=torch.matmul(X, w) +b

    # 生成y = Xw + b matmul更通用，支持向量，矩阵，批量矩阵乘法

    Y += torch.normal(0, 0.01, Y.shape) # 加入噪声

    return X, Y.reshape((-1, 1))

  

true_w = torch.tensor([2, -3.4]) # 真实权重

true_b = 4.2 # 真实偏差

features, labels = synthetic_data(true_w, true_b, 1000) # 生成数据集

# 用图片展示一下分布

d2l.set_figsize()

d2l.plt.scatter(features[:, 1].detach().numpy(), labels.detach().numpy(), 1);
   ```
![屏幕截图 2026-08-31 003031](./图片/屏幕截图%202026-08-31%20003031.png)
   
   定义一个data_iter函数，该函数接受批量大小，特征矩阵和标签向量作为输入，生成大小为batch_size的小批量
```
def data_iter(batch_size, features, labels):

    num_examples = len(features)

    indices = list(range(num_examples)) # 生成一个从0到num_examples-1

    random.shuffle(indices) #将indices随机打乱，随机访问这些样本

    for i in range(0,num_examples, batch_size):# 步长batch_size

        '''从打乱的样本列表中截出来一段batch长度的数据'''

        batch_indices = torch.tensor(indices[i:min(i+batch_size,num_examples)]) # 最后一次可能不足一个batch

        yield features[batch_indices], labels[batch_indices] # 生成一个batch的特征和标签

  

batch_size = 10

for X, y in data_iter(batch_size, features, labels):

    print(X, '\n', y)

    break
//tensor([[-0.2787, -2.2050],
        [ 0.1182,  0.8653],
        [-0.2239, -0.0566],
        [ 0.3705, -1.2929],
        [-0.2704,  0.6568],
        [ 0.9577,  0.7708],
        [-1.1985, -0.4521],
        [ 0.6915,  0.8702],
        [ 0.1554, -1.5120],
        [ 0.0787, -0.6160]]) 
 tensor([[11.1317],
        [ 1.5115],
        [ 3.9237],
        [ 9.3269],
        [ 1.4173],
        [ 3.4990],
        [ 3.3236],
        [ 2.6228],
        [ 9.6705],
        [ 6.4579]])
```
数据集完成后，我们需要初始化模型参数,定义初始的模型
```
w = torch.normal(0, 0.01, size=(2,1), requires_grad=True) # 初始化权重

b = torch.zeros(1, requires_grad=True) # 初始化偏差

  

# 定义初始模型

def linreg(X, w, b):

    """线性回归模型"""

    return torch.matmul(X, w) + b

#定义损失函数

def square_loss(y_hat, y):

    """均方损失"""

    return (y_hat - y.reshape(y_hat.shape)) ** 2 / 2

  

#定义优化算法

def sgd(params, lr, batch_size):

    """小批量随机梯度下降"""

    # with torch.no_grad()表示在该代码块中不需要计算梯度

    with torch.no_grad():

        for param in params:

            param -= lr * param.grad / batch_size # 更新参数

            param.grad.zero_() # 清空梯度
```
接下来进行训练
```
lr = 0.03 # 学习率

num_epochs =3 # 迭代次数

net = linreg  

loss = square_loss

  

for epoch in range(num_epochs):

    for X,y in data_iter(batch_size,features,labels):

        l = loss(net(X, w, b),y)

        l.sum().backward()

        sgd([w, b],lr,batch_size)

    with torch.no_grad():

        train_l = loss(net(features,w,b),labels)

        print(f'epoch {epoch + 1}, loss {train_l.mean().item():.7f}')

//
epoch 1, loss 0.0000476
epoch 2, loss 0.0000476
epoch 3, loss 0.0000476
```

## 线性回归用库简易实现
```
%matplotlib inline

import random

import torch

from torch.utils import data

# d2l是一个深度学习的入门教学库

from d2l import torch as d2l

  

true_w = torch.tensor([2,-3.4])

true_b = 4.2

features,labels = d2l.synthetic_data(true_w, true_b,1000)


def load_array(data_arrays,batch_size,is_train=True):

    """构造一个pytorch数据迭代器"""

    #dataset是一个数据集的抽象类，TensorDataset是它的子类

    #data.TensorDataset(*data_arrays)将数据数组包装成一个数据集

    dataset = data.TensorDataset(*data_arrays)

    return data.DataLoader(dataset,batch_size,shuffle=is_train)

  

batch_size = 10

data_iter = load_array((features,labels),batch_size)

  

# 首先把data_iter转换为一个可迭代对象，然后使用next函数获取第一个小批量数据

next(iter(data_iter))

//[tensor([[ 1.4287, -0.1611],
         [ 0.3241, -0.5816],
         [-1.1480,  1.1859],
         [-1.4006, -1.1043],
         [ 0.3059,  1.0647],
         [ 1.8374, -0.1195],
         [-1.0890,  1.0964],
         [-0.7666,  0.2141],
         [-0.6208,  0.5570],
         [ 0.1786,  2.3949]]),
 tensor([[ 7.6163],
         [ 6.8423],
         [-2.1414],
         [ 5.1791],
         [ 1.1721],
         [ 8.2686],
         [-1.7077],
         [ 1.9431],
         [ 1.0563],
         [-3.5875]])]


from torch import nn # nn是神经网络的缩写

# 输入维度是2，输出维度是1

net=nn.Sequential(nn.Linear(2,1)) # Sequential是一个容器，里面的模块将按照传入的顺序依次被添加到计算图中

net[0].weight.data.normal_(0,0.01) # 初始化权重参数

net[0].bias.data.fill_(0) # 初始化偏置参数//from torch import nn # nn是神经网络的缩写
# 输入维度是2，输出维度是1
net=nn.Sequential(nn.Linear(2,1)) # Sequential是一个容器，里面的模块将按照传入的顺序依次被添加到计算图中
net[0].weight.data.normal_(0,0.01) # 初始化权重参数
net[0].bias.data.fill_(0) # 初始化偏置参数

loss = nn.MSELoss() # 均方误差损失函数

trainer = torch.optim.SGD(net.parameters(),lr=0.03) # 随机梯度下降优化器

num_epochs = 3

for epoch in range(num_epochs):

    for x,y in data_iter:

        l=loss(net(x),y) # 计算损失

        trainer.zero_grad() # 梯度清零

        l.backward() # 反向传播计算梯度

        trainer.step() # 更新参数

    l = loss(net(features),labels) # 计算在整个数据集上的损失

    print(f'epoch {epoch+1}, loss {l:f}') # 输出当前的损失

//
epoch 1, loss 0.000222
epoch 2, loss 0.000098
epoch 3, loss 0.000098


```

# 基础优化方法
## 梯度下降（最直观）
1.挑选一个参数初始值$w_{0}$ 
2.重复迭代参数t=1，2，3
	$w_{t} =w_{t-1}-\epsilon\frac{\partial l}{\partial w_{t-1}}$
3.沿梯度方向将增加损失函数值
4.学习率：步长的超参数$\epsilon$,不能太小，也不能太大。
## 小批量随机梯度下降（最常见）
1.在整个训练集上算梯度太贵，一个深度神经网络模型可能需要数分钟至数小时
2.我们可以随机采样b个样本$i_{1},i_{2},i_{3},i_{b}$ 来近似损失
	$\frac{1}{b}\sum_{}^{}l(x_{i},y_{i},w)$
3.b是批量大小，另一个重要的超参数，不能太大，也不能太小。计算太小，不适合并行来最大利用计算资源；不能太大，内存消耗增加，浪费计算。

# Softmax线性回归
## 回归VS分类
1.回归：估计一个连续值
		· 单连续数值输出
		· 自然区间R
		· 跟真实值的区别作为损失
2.分类：预测一个离散类别，比如MNIST（手写数字识别），ImageNet（自然物体分类）
		·通常多个输出
		·输出i是预测为第i类的置信度
## 从回归到多类分类
### 均方损失
![屏幕截图 2026-09-02 210724](./图片/屏幕截图%202026-09-02%20210724.png)
### 无校验比例
![屏幕截图 2026-09-02 211311](./图片/屏幕截图%202026-09-02%20211311.png)
### 校验比例
![屏幕截图 2026-09-02 212321](./图片/屏幕截图%202026-09-02%20212321.png)
### 交叉熵损失
![屏幕截图 2026-09-02 214123](./图片/屏幕截图%202026-09-02%20214123.png)
总结：
softmax回归是一个多类分类模型，使用softmax操作子得到每个类的预测置信度，使用交叉熵来衡量预测和标号的区别。

## Softmax回归从0开始实现
利用fashion-minist数据集[[数据处理#图像分类数据集]]

```
import torch

from IPython import display

from d2l import torch as d2l

  

batch_size = 256

train_iter, test_iter = d2l.load_data_fashion_mnist(batch_size)

  

num_inputs = 784

num_outputs = 10

  

w=torch.normal(0, 0.01, size=(num_inputs, num_outputs), requires_grad=True)

b=torch.zeros(num_outputs, requires_grad=True)

  

def softmax(X):

    X_exp = torch.exp(X)

    partition = X_exp.sum(1, keepdim=True)# 按行求和，结果是一个列向量，其第i行是X的第i行元素的和

    return X_exp / partition  # 这里应用了广播机制

  

X = torch.normal(0, 1, (2, 5))

X_prob = softmax(X)

X_prob,X_prob.sum(1)
//(tensor([[0.0249, 0.4274, 0.0862, 0.1623, 0.2993],
         [0.0122, 0.0753, 0.3534, 0.3801, 0.1791]]),
 tensor([1., 1.]))
 
 def net(X):

    """ 实现softmax回归模型"""

    #-1表示这一维由pytorch自动计算得出

    return softmax(torch.matmul(X.reshape((-1, w.shape[0])), w) + b)

```
pytorch中有一种高级索引表达。y_hat[[0,1], y] 是 PyTorch 的高级索引，用于从预测概率矩阵中取出指定样本的真实类别概率。

假设：

y_hat = tensor([

    [0.1, 0.7, 0.2],

    [0.8, 0.1, 0.1]

])

y = tensor([1, 0])

那么y_hat[[0,1], y] 等价于torch.tensor([

    y_hat[0, 1],  # 第0张图片，真实类别为1

    y_hat[1, 0]   # 第1张图片，真实类别为0

])

结果是tensor([0.7, 0.8])

```
y=torch.tensor([0, 2])

y_hat = torch.tensor([[0.1, 0.3, 0.6], [0.3, 0.2, 0.5]])

y_hat[[0, 1], y]//tensor([0.1000, 0.5000])

def cross_entropy(y_hat, y):

    """交叉熵损失函数"""

    return -torch.log(y_hat[range(len(y_hat)), y])
    
cross_entropy(y_hat, y)//tensor([2.3026, 0.6931])

def accuracy(y_hat, y):

    """计算预测正确的数量"""

    if len(y_hat.shape) > 1 and y_hat.shape[1]>1:

        y_hat = y_hat.argmax(axis=1)

    cmp =y_hat.type(y.dtype) == y

    return float(cmp.type(y.dtype).sum())

  

accuracy(y_hat, y)/len(y)//0.5

def evaluate_accuracy(net, data_iter):

    """计算在指定数据集上模型的精度

        评估任意模型上的准确度

    """

    if isinstance(net, torch.nn.Module):

        net.eval() # 将模型设置为评估模式

    metric = d2l.Accumulator(2) #这一步是创建一个累加器，保存两个数据： 正确预测数、预测总数

    with torch.no_grad():

        for X, y in data_iter:

            metric.add(accuracy(net(X), y), y.numel())# y.numel()返回y中元素的个数

    return metric[0] / metric[1]

def train_epoch_ch3(net, train_iter, loss, updater):

    """训练模型一个迭代周期（定义见第3章）"""

    # 将模型设置为训练模式

    if isinstance(net, torch.nn.Module):

        net.train()

    metric = d2l.Accumulator(3) # 训练损失总和、训练准确度总和、样本数

    for X, y in train_iter:

        # 计算梯度并更新参数

        y_hat = net(X)

        l = loss(y_hat, y)

        if isinstance(updater, torch.optim.Optimizer):

            # 使用PyTorch内置的优化器和损失函数

            updater.zero_grad()

            l.backward()

            updater.step()

            metric.add(float(l) * len(y), accuracy(y_hat, y), y.numel())

        else:

            # 使用定制的优化器和损失函数

            l.sum().backward()

            updater(X.shape[0])

            metric.add(float(l.sum()), accuracy(y_hat, y), y.numel())

    return metric[0] / metric[2], metric[1] / metric[2]

class Animator:  #@save

    """在动画中绘制数据"""

    def __init__(self, xlabel=None, ylabel=None, legend=None, xlim=None,

                 ylim=None, xscale='linear', yscale='linear',

                 fmts=('-', 'm--', 'g-.', 'r:'), nrows=1, ncols=1,

                 figsize=(3.5, 2.5)):

        # 增量地绘制多条线

        if legend is None:

            legend = []

        d2l.use_svg_display()

        self.fig, self.axes = d2l.plt.subplots(nrows, ncols, figsize=figsize)

        if nrows * ncols == 1:

            self.axes = [self.axes, ]

        # 使用lambda函数捕获参数

        self.config_axes = lambda: d2l.set_axes(self.axes[0], xlabel, ylabel,

                                                xlim, ylim, xscale, yscale, legend)

        self.X, self.Y, self.fmts = None, None, fmts

  

    def add(self, x, ys):

        """向图中添加一个或多个数据点"""

        if not hasattr(ys, '__len__'):

            ys = [ys]

        self.X = self.X or [[] for _ in ys]

        self.Y = self.Y or [[] for _ in ys]

        for x_value, y_value, x_list, y_list in zip([x] * len(ys), ys, self.X, self.Y):

            x_list.append(x_value)

            y_list.append(y_value)

        self.axes[0].cla()

        for x_values, y_values, fmt in zip(self.X, self.Y, self.fmts):

            self.axes[0].plot(x_values, y_values, fmt)

        self.config_axes()

        display.display(self.fig)

        display.clear_output(wait=True)

def train_ch3(net, train_iter, test_iter, loss, num_epochs, updater):

    """训练模型"""

    animator = Animator(xlabel='epoch', xlim=[1, num_epochs], ylim=[0.3, 0.9],

                        legend=['train loss', 'train acc', 'test acc'])

    for epoch in range(num_epochs):

        train_metrics = train_epoch_ch3(net, train_iter, loss, updater)

        test_acc = evaluate_accuracy(net, test_iter)

        animator.add(epoch + 1, train_metrics + (test_acc,))

    train_loss,train_acc = train_metrics

    assert train_loss < 0.5, train_loss

```
```
lr = 0.1

def updater(batch_size):

    return d2l.sgd([w, b], lr, batch_size)

  

num_epochs = 10

train_ch3(net, train_iter, test_iter, cross_entropy, num_epochs, updater)
```
![屏幕截图 2026-09-03 184751](./图片/屏幕截图%202026-09-03%20184751.png)

```
def predict_ch3(net, test_iter, n=6):  #@save

    """预测标签（定义见第3章）"""

    for X, y in test_iter:

        break

    trues = d2l.get_fashion_mnist_labels(y)

    preds = d2l.get_fashion_mnist_labels(net(X).argmax(axis=1))

    titles = [true +'\n' + pred for true, pred in zip(trues, preds)]

    d2l.show_images(

        X[0:n].reshape((n, 28, 28)), 1, n, titles=titles[0:n])

  

predict_ch3(net, test_iter)
```
![屏幕截图 2026-09-03 184833](./图片/屏幕截图%202026-09-03%20184833.png)

## Softmax简易实现
```
import torch

from torch import nn

from d2l import torch as d2l

batch_size = 256

train_iter,test_iter = d2l.load_data_fashion_mnist(batch_size)

  
#pytorch 不会隐式的调整输入的形状

# 因此，我们定义了展平层，在线性层前调整网络输入的形状

net = nn.Sequential(nn.Flatten(), nn.Linear(784, 10))

  

def init_weights(m):

    if type(m) == nn.Linear:

        nn.init.normal_(m.weight, std=0.01)

#会递归遍历 net 中的所有子模块，并对每个模块调用 init_weights

net.apply(init_weights)

#在交叉熵损失函数中传递未归一化的预测，并同时计算softmax及其对数

loss = nn.CrossEntropyLoss()

#使用学习率为 0.1 的小批量随机梯度下降作为优化算法

trainer = torch.optim.SGD(net.parameters(), lr=0.1)

#训练模型

num_epochs = 10

d2l.train_ch3(net,train_iter,test_iter,loss,num_epochs,trainer)
```
# 损失函数
## 1.均方损失（L2-Loss）
公式
	$l(y,y^{'})=\frac{1}{2}(y-y^{'})^{2}$
![屏幕截图 2026-09-03 011137](./图片/屏幕截图%202026-09-03%20011137.png)
## 2.绝对值损失函数（L1-Loss)
公式
	$l(y,y^{'}=|y-y^{'}|)$
![屏幕截图 2026-09-03 011210](./图片/屏幕截图%202026-09-03%20011210.png)
## 3.鲁棒损失（Huber' Robust Loss）
![屏幕截图 2026-09-03 011327](./图片/屏幕截图%202026-09-03%20011327.png)

# 感知机
## 感知机定义
感知机就是一个二分类的问题，最后可以输出为0或1，1或-1
![[./图片/屏幕截图 2026-09-04 200417.png]]
感知机vs线性回归：回归输出的是实数，感知机输出的是一个类
感知机vs softmax：softmax输出的是一个概率。
## 训练感知机
只有$ y_{i}[<w_{i},x_{i}>+b] >=0才是正确的，说明是正类。如果是<0就说明我们分类错了，就需要不断去修正。所以他的停止条件是所有都分类正确
![[./图片/屏幕截图 2026-09-04 205220.png]]

## 收敛定理

![[./图片/屏幕截图 2026-09-04 210728.png]]

## XOR问题
感知机不能拟合XOR函数，他只能产生线性分割面。
![[./图片/屏幕截图 2026-09-04 210956.png]]
总结：
1.感知机是一个二分类模型，是最早的ai模型之一。
2.他的求解算法等价于使用批量大小为1的梯度下降。
3.他不能拟合XOR函数，导致第一次AI寒冬。
## 多层感知机

可以解决XOR问题[[机器学习#XOR问题]]，XOR就是异或。就是一层解决不了的问题，我们多层来解决
![[./图片/屏幕截图 2026-09-04 220342.png]]
## 隐藏层
隐藏层的大小是一个超参数
![[./图片/屏幕截图 2026-09-04 220747.png]]
1.输入$x\in R^{n}$
2.隐藏层$W_{1}\in R^{m*n},b_{1}\in R^{m}$
3.输出层，$w_{2}\in R^{m},b_{2}\in R$
		$h=\sigma(w_{1}x+b_{1})$
		$o=w_{2}^{T}h+b_{2}$
	$\sigma$ 是元素的激活函数（非线性

		
		




